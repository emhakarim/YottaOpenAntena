# Pinned analysis dependencies (optional - the core needs none of these).
#
# Install with:  .venv\Scripts\python.exe -m pip install -r requirements-lock.txt
#
# Verified together on Windows with CPython 3.13.15.

numpy==2.5.3
scipy==1.18.1
matplotlib==3.11.2
pytest==9.1.1
scikit-rf==2.1.0
h5py==3.16.0

# Desktop GUI (Phase 3). Optional: the core and the tests run without it.
PySide6-Essentials==6.11.2

# The CSXCAD and openEMS Python bindings are NOT on PyPI and are NOT pinned
# here: they are shipped inside the official openEMS Windows package and must be
# installed from that package's own wheels, which are built per Python version:
#
#   <openEMS>\python\csxcad-<ver>-cp3XX-cp3XX-win_amd64.whl
#   <openEMS>\python\openems-<ver>-cp3XX-cp3XX-win_amd64.whl
#
# scripts\bootstrap.ps1 installs the matching wheel automatically when
# OPENEMS_ROOT is set.
