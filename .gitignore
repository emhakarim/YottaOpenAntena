# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Third-party solver installs and downloads - never commit these
tools/

# Simulation output. Run directories are small (project.json, sim.py,
# run_manifest.json, s11.csv) but they are not source: share them explicitly
# or keep them on a results branch instead of committing them by default.
runs/

# Local scratch / logs
*.log
scratch/

# PyInstaller output: hundreds of MB of build artefacts, reproducible from the spec.
# Commit packaging/openantenna-gui.spec and scripts/build_gui.py, never the output.
build/
dist/
